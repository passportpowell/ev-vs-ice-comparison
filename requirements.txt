pandas>=2.2
numpy>=1.26
scipy>=1.12
scikit-learn>=1.4
pandera>=0.20
pillow>=10.0
# CPU-only torch wheels: install via the PyTorch index URL on CI:
#   pip install torch>=2.1 --index-url https://download.pytorch.org/whl/cpu
torch>=2.1
pytest>=8.0
